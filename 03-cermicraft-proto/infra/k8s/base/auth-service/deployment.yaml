apiVersion: apps/v1
kind: Deployment
metadata:
  name: auth-service
  namespace: ceramicraft
spec:
  replicas: 2
  selector:
    matchLabels:
      app: auth-service
  template:
    metadata:
      labels:
        app: auth-service
    spec:
      containers:
      - name: auth-service
        image: nginxdemos/hello
        ports:
        - containerPort: 80
